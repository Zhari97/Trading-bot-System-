const esc=v=>String(v??'—').replace(/[&<>]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;'}[c]));
const date=v=>v?new Date(v).toLocaleString('it-IT'):'—';
const num=v=>v==null?'—':Number(v).toFixed(2);
const pct=v=>v==null?'—':Number(v).toFixed(2)+'%';
const badge=v=>`<span class="badge ${String(v??'WATCH').replace(' ','-')}">${esc(v??'WATCH')}</span>`;
const getJson=async u=>{const r=await fetch(u,{cache:'no-store'});if(!r.ok)throw Error(`${u}: ${r.status}`);return r.json()};

function card(m){return `<article class="market"><div class="card-head"><h3>${esc(m.pair)}</h3>${badge(m.classification)}</div><div class="price">${esc(m.price)}</div><div>${esc(m.direction)} · <span class="score">Score ${esc(m.score)}/100</span></div><div class="market-meta">Confluenza ${esc(m.confluence)}% · RSI ${esc(m.rsi)} · Trend ${esc(m.categories?.trend)} · Momentum ${esc(m.categories?.momentum)}</div></article>`}
function graph(h){if(!Array.isArray(h)||!h.length)return;const p=h.slice(-70),min=Math.min(...p.map(x=>Number(x.score)||0)),max=Math.max(...p.map(x=>Number(x.score)||0)),span=Math.max(1,max-min);document.querySelector('#chart').innerHTML=`<polyline class="line" points="${p.map((x,i)=>`${i*(760/Math.max(1,p.length-1))},${205-((Number(x.score)||0)-min)*185/span}`).join(' ')}"/>`}
function event(text,tag='SYSTEM'){const box=document.querySelector('#events');if(box.querySelector('.muted'))box.innerHTML='';const d=document.createElement('div');d.className='event';d.innerHTML=`<span class="event-time">${new Date().toLocaleTimeString()}</span><span class="event-tag">${esc(tag)}</span> ${text}`;box.prepend(d);while(box.children.length>28)box.lastChild.remove()}
function setPipeline(q){const regime=q?.classification||q?.categories?.trend||'—',signal=q?.direction||'—',score=q?.score??'—',risk=q?.guard_rail?.status||'—';document.querySelector('#pipe-regime').textContent=regime;document.querySelector('#pipe-signal').textContent=signal;document.querySelector('#pipe-score').textContent=score;document.querySelector('#pipe-risk').textContent=risk;document.querySelector('#pipe-decision').textContent=q?.telegram?'ALERTED':(q?.classification||'WATCH');}

async function refresh(){
 const [s,m,h,x,r]=await Promise.all([getJson('/api/status'),getJson('/api/markets'),getJson('/api/history'),getJson('/api/signals'),getJson('/api/research')]);
 const markets=Array.isArray(m)?m:(m?.markets&&typeof m.markets==='object'?m.markets:m);const history=Array.isArray(h)?h:(Array.isArray(h?.history)?h.history:[]);const signals=Array.isArray(x)?x:(Array.isArray(x?.signals)?x.signals:[]);const a=Object.values(markets||{});const last=history.at(-1);const q=a[0]||signals[0];
 document.querySelector('#timeframe').textContent=s?.timeframe_minutes!=null?`${s.timeframe_minutes}m`:'15m';document.querySelector('#updated').textContent=date(s?.updated_at||last?.timestamp);document.querySelector('#status').textContent='● '+(a.length?'ONLINE':'NO DATA');document.querySelector('#engine-state').textContent=a.length?'ACTIVE':'WAITING';document.querySelector('#engine-detail').textContent=date(s?.updated_at||last?.timestamp);
 document.querySelector('#markets').innerHTML=a.map(card).join('')||'<div class="muted">Nessuna analisi persistita.</div>';
 if(q){setPipeline(q);document.querySelector('#live-summary').innerHTML=`<strong>${esc(q.pair||'MARKET')}</strong><br>${esc(q.direction||'NEUTRO')} · <span class="score">Score ${esc(q.score??'—')}/100</span><br><small>Guard-rail: ${esc(q.guard_rail?.status||'—')} · Confluenza: ${esc(q.confluence??'—')}%</small>`;event(`${esc(q.pair||'MARKET')} · ${esc(q.direction||'—')} · score ${esc(q.score??'—')}`,q.classification||'SIGNAL')}
 document.querySelector('#signals').innerHTML=signals.map(q=>`<tr><td>${date(q.timestamp)}</td><td>${esc(q.pair)}</td><td>${badge(q.classification)}</td><td>${esc(q.direction)}</td><td>${esc(q.score)}</td><td>${esc(q.confluence)}%</td><td>${esc(q.guard_rail?.status)}</td><td>${esc(q.telegram)}</td></tr>`).join('')||'<tr><td colspan="8">Nessun dato persistito.</td></tr>';
 graph(history);
 const tf=r?.timeframes||{},rows=Object.entries(tf),one=tf['1h'];document.querySelector('#research-meta').textContent=r?.generated_at?`Generato: ${date(r.generated_at)} · ${esc(r.symbol)} · ${esc(r.history_days)} giorni · allocazione ${num(r.allocation_pct)}%`:'Nessun risultato pubblicato.';document.querySelector('#research-rows').innerHTML=rows.map(([name,v])=>{const o=v.oos||{};return `<tr><td><b>${esc(name)}</b></td><td>${esc(o.signals)}</td><td>${pct(o.win_rate_pct)}</td><td>${num(o.profit_factor)}</td><td>${pct(o.expectancy_pct)}</td><td>${pct(o.max_drawdown_pct)}</td></tr>`}).join('')||'<tr><td colspan="6">Nessun risultato pubblicato.</td></tr>';
 document.querySelector('#research-chart').innerHTML=rows.map(([name,v])=>{const value=Number(v.oos?.portfolio_return_pct??v.oos?.return_pct??0),height=Math.max(8,Math.min(85,Math.abs(value)*20));return `<div class="research-bar ${value<0?'negative':''}" title="${esc(name)}: ${value.toFixed(2)}%" style="height:${height}px"></div>`}).join('');
 if(one){document.querySelector('#oos-return').textContent=pct(one.oos?.portfolio_return_pct??one.oos?.return_pct);document.querySelector('#oos-pf').textContent=num(one.oos?.profit_factor);document.querySelector('#research-state').textContent=one.signal_relationship||one.stability?.signal_relationship||'—';}
}
async function safeRefresh(){try{await refresh()}catch(e){console.error(e);document.querySelector('#status').textContent='● DATA ERROR';document.querySelector('#status').className='bad'}}
safeRefresh();setInterval(safeRefresh,30000);
